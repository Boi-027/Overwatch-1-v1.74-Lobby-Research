================================================================================
  Overwatch 1.74.0.0 - Local Preservation Server: Project State & Handoff
  Build 104319, Windows x64 (retail product, _retail_ folder, Product=pro)
  Last updated: 2026-09-01 (rev 19)
================================================================================

MAIN GOAL
---------
Reach the main menu + lootbox screen on a fully local/offline server.
No gameplay, no matchmaking. Game preservation, own machine, own copy.

CONVENTIONS USED IN THIS DOCUMENT
---------------------------------
<GAME>   = your Overwatch 1.74 install root, containing Overwatch\_retail_\
<TOOLS>  = wherever you keep lobbyserv.py, cainj.cpp, owobs.cpp, etc.
<LOGS>   = a directory you create for --tank_logpath output
<DUMPS>  = a directory for owobs region dumps

ADDRESS CONVENTION - CORRECTED (rev 18)
  Addresses in this document are RVAs (offsets from the module base). In a
  pe-sieve process dump they are ALSO the file offsets - verified: the
  Success() signature 48 83 C1 F0 BA 07 sits at file offset 0xE38600, which
  is the value this document calls RVA 0xE38600. Do NOT apply a section
  delta when reading the dump with a hex editor or a script. (The PE section
  headers claim raw != virtual; empirically the dump does not follow them.)
  Runtime address = module base + RVA.


################################################################################
##  READ THIS FIRST - CORRECTIONS TO EARLIER REVISIONS
################################################################################

Rev 18 invalidates several things asserted in rev 17 and earlier. If you have
an older copy, discard the following.

(1) THE VOICECHAT MISIDENTIFICATION (2026-08-25, still applies)
    0x86BEE0 "ResponseConnect", 0x86F432 "ResponseLogin", the names table at
    0x261AB68, and the pending-connect-token theory all belong to the VOICE
    CHAT client, not the lobby. Full detail in Section 8 (VOID).
    LESSON: confirm which SUBSYSTEM a handler belongs to before investing.
    Dump the whole names table. If the neighbours are audio devices, stop.

(2) 0x41DB60 IS NOT A MESSAGE DISPATCHER  ★ NEW, rev 18
    Rev 17 described it as the JAM dispatcher with a "handler list" at
    conn+0x250. It is the OUTBOUND SEND-QUEUE ENQUEUE. See Section 3.
    Consequently:
      - conn+0x250 is a TX buffer chain, not a handler list.
      - The "msgid <= 0x4F0" gate is a MAX WRITE SIZE of 1264 bytes. The
        value tested is a LENGTH, not a msgid.
      - Rev 17 Section 4 items 1 and 2 ("find the lobby handler table",
        "find the queue drainer") were chasing the transmit path. VOID.
      - The 2E sweep of "1,264 candidate msgids" got its size from a byte
        cap by coincidence.

(3) +0x439904 IS NOT A DRAIN+DISPATCH CORE  ★ NEW, rev 18
    Rev 17 Section 2V described it as the drain-and-dispatch core with a
    "cmp [rcx+0x20], 0x32" gate. Decompiled: it is a small bool predicate
    that checks a global flag, calls a generic accessor, does one virtual
    call as a type query, and tests obj+0x250 non-null. No dispatch, no list
    walk. The 0x32 gate does not exist - it came from a hand-rolled
    disassembler misreading bytes.

(4) THE 11-NAME "LOBBYSYSTEM HANDLER FAMILY" WAS FABRICATED  ★ NEW, rev 18
    Rev 17 Section 2V listed eleven LobbySystem_C::__* strings. Only FOUR
    such strings exist in the binary:
        LobbySystem
        LobbySystem_C::LobbyConnectHelloErrorHandler
        LobbySystem_C::LobbyDisconnectClientHandler
        ClientLobbyMessageSystem_C::__LobbyServerConnectionUp
    __LobbyServerLoginResponse, __LobbyServerHelloReceived, and the rest were
    never in the data. Any reasoning built on that flow is unfounded.
    Also: the only code reference to these strings is a logger with format
    "[%s] %s - %s". Dispatch is by id/vtable slot, not by name - searching
    for handlers by name will not work.

(5) .rdata IS DECRYPTED IN A PROCESS DUMP  ★ NEW, rev 18
    Rev 17 Section 5 says "USING DUMP .rdata ADDRESSES. Encrypted and shifted
    ~0x10000. Code only." That is FALSE for a pe-sieve dump of the running
    process. Measured: .rdata entropy 5.713, 27% printable, lobby strings
    readable in the clear. The live-.rdata-snapshot tooling (owobs v49-v52)
    is no longer needed for static work.
    .data IS truncated in the dump (0x24B800 of 0x83E5A4), so .data addresses
    past ~0x2EE3600 are absent. .text and .rdata are complete.

(6) THE CIPHER-DESYNC THEORY IS DEAD  ★ NEW, rev 18
    Rev 17 Section 2M proposed a JAM counter desync on content frames.
    Disproved twice over:
      - The entire 383,890-byte retail server stream decrypts in ONE
        continuous pass, 94 frames, zero parse errors. The s2c keystream is
        continuous across the whole session; there is no per-frame reset.
      - Answering every poll with a bare frame stays in sync for 40+
        consecutive frames.
    When a content frame closes the client it is not cipher state.

(7) HOW THE 2E SWEEP MUST BE READ  ★ NEW, rev 18
    That sweep REPLACED the ack with the candidate msgid. A msgid scoring 0
    there means "this is not the ack", NOT "this msgid is rejected on its
    merits" - the client tore down because it was never acked at all.
    2E establishes exactly one thing: 0x0102 is the ack.

(8) srv_dec.bin DOES NOT PARSE FROM BYTE 0  ★ NEW, rev 18
    The README's reading snippet works on cli_dec.bin and fails on
    srv_dec.bin. The server stream parses only from offset 292:
        off = 292
    From there: exactly 94 frames, 383890/383890, zero errors. The first 292
    bytes are the decrypted state blob.


################################################################################
##  SECTION 1: SOLVED AND VERIFIED
################################################################################

[SOLVED] JAM cipher - reimplemented server-side in lobbyserv.py (Jam class).
  Known-answer test (independently confirmed by two other implementers):
    key    = 00 01 02 ... 1f
    first 64 bytes of keystream:
      f07e5df1c8d51c0d754da6ddde34cca01db0ed26334eb94ae277135a0764c6a6
      794d7ab0cc0978ef43de30766a7ee1d7c90e159df239d4016d450fa3d8207842

[SOLVED] Key schedule - VERIFIED END TO END against a real capture.
    MAC1            = HMAC-SHA256(k0, cn || sn)     (client -> server)
    MAC2            = HMAC-SHA256(k1, sn || cn)     (server -> client)
    c2s cipher key  = HMAC-SHA256(k3, sn || cn)
    s2c cipher key  = HMAC-SHA256(k2, cn || sn)
  Four 64-byte key slots from the Battle.net session. Tournament mode zeroes
  all four, which is why ZERO_KEY works locally.
  Verification (rev 18): using the k0-k3 published with packets.pcap, MAC1
  verifies with k0, MAC2 with k1, and the blob decrypts BYTE-IDENTICAL to the
  first 292 bytes of srv_dec.bin. The schedule above is correct, not assumed.

[SOLVED] Handshake to state 6.
    C->S  "HELLO PRO CLIENT\0"                  (17 bytes)
    S->C  "HELLO PRO SERVER\0"                  (17 bytes)
    C->S  [u64 client id][32-byte cn]           (40 bytes)
    S->C  [32-byte challenge][1 byte difficulty][32-byte sn]   (65 bytes)
    C->S  [u64 counter][32-byte MAC1]           (40 bytes)
    S->C  MAC2 || JAM(blob)
  Field names confirmed independently by Sidiusz, who reconstructed the same
  layout. The first 8 bytes of frame 1 are a CLIENT ID (the value published
  alongside packets.pcap as "Client ID: 16390422107773322648"). The first 8
  bytes of frame 3 are a PROOF-OF-WORK COUNTER whose size scales with the
  difficulty byte - measured: difficulty 7 -> counter 944, difficulty 9 ->
  counter 180. lobbyserv sends difficulty 0, which the client accepts with no
  work required.

  CRITICAL: MAC2 and the blob are ONE CONTINUOUS cipher stream. Do not
  reinitialize JAM between them; the partial-block keystream must carry over.
  Encrypt all 292 blob bytes in the same stream, offset 0, no header.
  Getting this wrong produces a client that decrypts the blob, counts all 292
  bytes, and silently closes - a very confusing failure mode. (Sidiusz lost
  time to exactly this; so did we.)

[SOLVED] 292-byte state blob (TOURNAMENT MODE ONLY).
  BLOB_KEY (64 bytes):
    35 86 F3 62 8A 63 1B 70 57 12 40 5B 8A CC 71 D4
    0F D1 67 0C C1 B0 3E A3 84 97 4A 6F B1 A7 61 96
    B1 42 F0 B7 23 10 EA 81 16 D0 0A 4C 35 2F 09 AC
    DB FB 50 A6 3E C5 15 3E 62 E4 D6 7F E0 9B EE CC
  Construction:
    256-byte body, body[i] = i & 0xFF
    body[0]        = 0x02
    body[1:5]      = 7F 00 00 01        (127.0.0.1)
    body[255]      = 0xFF               (forces blob >= n, RSA path skipped)
    body[176:208]  = HMAC-SHA256(BLOB_KEY, body[0:176] + body[208:256])
    + 36 trailing zero bytes            => 292 bytes
  Sidiusz's independent implementation is byte-identical, including the 36
  zero trailer, and he confirms zero is sufficient to reach state 6.
  NOTE: the REAL blob is session-specific RSA material - in the decrypted
  capture bytes 0..255 are high entropy and the 36-byte trailer is structured
  (3388fd09 00050001 8469000000000000 / 3388fd09 000d0001 00008c0e /
  0a394366 cc680100). Our construction is a tournament-mode shortcut that
  works only because zeroed keys let the client skip validating it.

[SOLVED] Counter gate on the first reply.
  conn+0x88 initializes to 1 and a reply's index must be BELOW it, so the
  first server reply must carry counter = 0. (01 00 00 00 encodes 1, not 0 -
  this off-by-one silently killed every reply for weeks.)

[SOLVED] Injection chain that survives Arxan.
  bink2w64.dll (proxy) -> loads bink2w64_original.dll + your DLLs.
  ALL ~75 Bink exports must be forwarded via
    #pragma comment(linker, "/export:NAME=bink2w64_original.NAME")
  A single missing export produces "Failed to load" before your code runs.
  Verify with: dumpbin /exports bink2w64.dll

[SOLVED] Arxan constraints - what works and what does not.
  - VirtualProtect on Overwatch.exe .text: BLOCKED. 11,408 of 11,408 pages
    denied, err=87. Inline hooking is impossible inside the game module.
    MH_EnableHook returns error 10 (MH_ERROR_MEMORY_PROTECT).
    Sidiusz independently hit the same wall (ACG / STATUS_INVALID_PAGE_
    PROTECTION on rwx/rw/writecopy) and reached the same conclusion.
  - Inline hooking of EXTERNAL modules (winmm.dll etc.) works.
  - HEAP WRITES work with no Arxan reaction.
  - CALLING game functions from an injected thread WORKS. Proven twice:
    Success() at 0xE38600, and Keyring::LoadFromBuffer at 0x391DD0 with a
    constructed 9.5 KB argument (Section 6). This is the single most useful
    capability we have.
  - SuspendThread trips the watchdog.
  - Hardware breakpoints (DR registers) are detected; even a one-shot
    arm-then-disarm kills the process. DEAD, do not retry.
  - x64dbg + ScyllaHide: defeated at every preset.

[SOLVED] .pdata gives exact function boundaries.  ★ NEW, rev 18
  The dump's exception directory sits at RVA 0x34D8000 (file offset the same)
  and yields 165,412 RUNTIME_FUNCTION entries (BeginAddress, EndAddress,
  UnwindData; 12 bytes each). This removes all guesswork about where
  functions start and end - use it for every disassembly. Parse:
      for i in range(0x1E4A10//12):
          b,e,u = struct.unpack_from('<III', B, 0x34D8000 + i*12)


################################################################################
##  SECTION 2: THE JAM MESSAGE SYSTEM - FULLY MAPPED  ★ NEW, rev 18
################################################################################

JAM = "Joe's Advanced Messages" (Joe Rumsey, presented publicly in 2013; used
in WoW and Overwatch, different versions). The structures below were derived
independently from the 1.74 dump and then cross-validated field-for-field
against Prometheus's JAM.h (github.com/BreakingBread0/prometheus), which
documents the same system on OW 0.8 beta.

JamMessageInfo  (the per-message registration record, 0x20 bytes)
    +0x00  char* message_name       (stripped in 1.74 - all point to one
                                     placeholder at RVA 0x25E1640)
    +0x08  char* message_protocol   (also stripped; printed as %x in 0.8's
                                     tooling, so it may be an id, not a name)
    +0x10  JamMessageItemInfo* items
    +0x18  u32 crc                  }  read together as a qword this is
    +0x1C  u32 message_id           }  (message_id << 32) | crc

JamMessageItemInfo  (per-field record; 0x30 stride in 1.74, 0x28 in 0.8)
    +0x00  char* name_str           (stripped in 1.74)
    +0x08  u32 message_type
    +0x0C  u32 offset               (relative to the JamMessage object)
    +0x10  u32 size
    +0x14  u32 has_extra_flag       (0xFFFFFFFF on the last field)
    +0x18  JamMessageItemInfo* inner_data_ptr   (nested struct layout)
    +0x20  void* "decoding_func"    -- MISNOMER, see below
    +0x28  (extra qword, 1.74 only; observed 0x20/0x80/0xA0)

  ★ The +0x20 pointer is NOT a decoder. Disassembled: the ones sampled are
    std::vector::resize thunks (this = {ptr,size}, arg = new count, scaling
    by 4 or 8 for the element type, shrink -> destroy-range, grow -> reserve
    + memset). It is present ONLY on ARRAY-typed fields and its presence is
    how you tell a repeated field from a scalar. `size` on such a field is
    the ELEMENT size.
    Consequence: lobby msg 1 is a REPEATED u32, not a scalar u32.
    ClientInLobbyConnect 20504/20505 are REPEATED doubles.

  ★ Terminator: a record with message_type == 17 (END) ends the list. Do not
    rely on has_extra_flag for termination - 0.8's own tooling loops
    `while (item->message_type != 0x11)`.

FIELD TYPE ENUM (authoritative, from Prometheus's jam_messagedetail_window.h)
     0        BOOL
     1,2,3    BYTE
     4,5      SHORT
     6,7      INT (u32)
     8,11     DOUBLE          <-- NOT a u64; corrected rev 18
     9        QWORD
    10        FLOAT
    12        STRING_ARR
    13        STRING
    14        STRUCT          (has inner_data_ptr)
    15        object?
    16        DYNAMIC
    17        END

JamPoolVtable - 1.74 LAYOUT  ★ CORRECTED rev 19
  ★ THE ADDRESS A POOL OBJECT STORES AT +0x00 IS 8 BYTES BELOW WHAT THE
    rev-18 VTABLE SCAN RECORDED. All slot numbers below are relative to the
    address objects actually hold. rev 18's table was built from the +8
    address and every slot index in it was one too low.
    lobby: objects store 0x2609D58; rev 18 recorded 0x2609D60.
    slot 0  destructor_1
    slot 1  destructor_outer
    slot 2  (per-protocol, small)
    slot 3  pool_crc()      -> u32     `mov eax,imm32; ret`
    slot 4  returns_zero()
    slot 5  min_msgId()     -> short
    slot 6  max_msgId()     -> short   (EXCLUSIVE)
    slot 7  allocate_pool(pool, msgId) -> JamMessage*
    slot 8  (shared across all pools, RVA 0x4CC9B0)
    slot 9  per-protocol op taking an existing JamMessage*
  Verified on two protocols:
    LOBBY    0x2609D58  crc=11D82194 min=1     max=5      alloc=0x56CF50
    ClientIn 0x25FEC40  crc=F5FF548A min=20500 max=20510  alloc=0x475380
  slot 7 locks pool+0xA8, allocates from pool+0x10, then switches on the
  msgId to construct the right message class.
  slot 9 calls 0x565260 (get_message_info()->message_id) on its rdx argument
  and switches on that, so it operates on an already-built message.

POOL OBJECT LAYOUT (rev 19, live)
    +0x00  vtable (see above)
    +0x08  u32 refcount/among two u32s
    +0x10  allocator; also a shared .data ptr identical across every pool
           (RVA 0x2E51610 - a hashed string table, NOT the subsystem)
    +0x18  u32 max message object size for this pool, then 0x80
           lobby 0xA0, ClientInLobbyConnect 0x158 - the latter matches
           msg 20500's schema extent (ends 0x157) exactly
    +0x28/+0x30, +0x38/+0x40  intrusive list heads (self-referential = empty)
    +0xA8  the pool's lock

PROTOCOL DESCRIPTOR (a struct, and also a message field type)
    +0x00  m_protocolCRC      u32
    +0x04  m_messageBase      u32
    +0x08  m_messageMax       u32   (EXCLUSIVE)
    +0x0C  m_protocolFlags    u32
    +0x10  m_untrustedServer  u32
    +0x18  m_protocolName     (0x28-byte string object)
  Field names live at RVA 0x2609E78..0x2609ED0. Total size 0x40.
  The lobby's instance is at RVA 0x2609CC8: CRC 0x11D82194, base 1, max 5,
  flags 0.

★ WIRE MSGID ENCODING
    wire_msgid = (channel << 8) | (message_id - pool_messageBase)
  Evidence: retail's decrypted server stream groups cleanly by high byte
  (0x1c02/0x1c05/0x1c06/0x1c09/0x1c0b/0x1c0c/0x1c0d/0x1c0e/0x1c14/0x1c15 is
  one pool; likewise 0x2a02/0x2a05/0x2a06 and 0x4e00/0x4e04/0x4e0d), and the
  1.74 lobby's message ids 1..4 appear on the wire as 0x0101..0x0104 with the
  lobby on channel 1. Retail runs the lobby on channel 0 (client sends
  0x0000/0x0003), so CHANNEL ASSIGNMENT IS DYNAMIC, not fixed per pool.

THE COMPLETE PROTOCOL MAP
  105 protocol pools were recovered with CRC and message range, by reading
  the vtable getters out of a live process (owobs v117/v118 dump + offline
  capstone). 922 message registrations with full field schemas were recovered
  from live .data (owobs v119). Both datasets are in the repo.
  Message ids are banded by hundreds: 20300-20323, 20400-20407, 20500-20510,
  20600-20603, 20700-20713, 20800-20826, 21600-21634, ... up to 60900-60907.
  Two pools are STATICALLY registered at startup:
      0x11D82194   ids 1..4        the lobby connect protocol
      0x9C08000F   ids 1000..1003  the console command protocol
                   (m_command, type 13, 0x400-byte buffer; names
                    CommandRequest / CommandOut / CommandAddConsole)
  A third, adjacent in memory and single-message:
      0x39FD5386   id 101 only     body = STRING_ARR + array + int16 +
                                   DYNAMIC + ProtocolDescriptor
  All the others exist as code (vtables + schemas) but are NOT registered at
  the lobby wall - see Section 4.

CLIENTINLOBBYCONNECT - pool 0xF5FF548A, ids 20500..20509
  This is the pool rev 17 Section 4 item 5 refers to (0.8 pushes ids
  20503/20504/20505 to drive DISCONNECTED -> CONNECTED -> menu). It EXISTS in
  1.74. Recovered schemas:
      20500  15 fields: 3x STRUCT(0x10) + 4x STRING_ARR(0x28) + 3 bools +
             2 u32 + 3 bools, body ends 0x157
      20501  STRUCT(0x10) @0x78, u32 @0x88, double @0x90
      20502  STRUCT(0x68) @0x78
      20503  double @0x78, bool @0x80
      20504  repeated double @0x78
      20505  repeated double @0x78
      20506  STRING_ARR(0x28) @0x78
      20507  bool @0x78
      20508  STRUCT(0x40) @0x78
      20509  double @0x78, u32 @0x80
  (Struct offsets. 0x00-0x77 is the common JamMessage header; payload starts
  at 0x78.)

★ THE WIRE ENCODING IS NOT THE STRUCT LAYOUT
  Tested: for retail messages whose fields are all fixed-size, the observed
  body length should equal the schema extent if bodies were flat struct
  dumps. 22 of 28 retail channels have NO candidate pool under that
  assumption. Combined with Section 5's finding that real bodies are
  [u32le block_len][block] sequences, the encoder does real serialisation.
  The schemas tell you what fields exist and in what order. They do NOT tell
  you how they serialise. Every body we constructed from flat struct layouts
  was wrong in form regardless of content.
  The generic encoder (the "binary decoding" path from the JamMessage
  metadata strings) has not been located; its strings have no resolvable
  xrefs.


################################################################################
##  SECTION 3: THE TRANSPORT LAYER - conn+0x250 IS THE SEND QUEUE
################################################################################

0x41DB60 - ENQUEUE FOR SEND (not a dispatcher)
    41DB60  test r8d,r8d / jle 41DF93         ; count <= 0 -> out
    41DB85  lea rsi,[rdx+0x10]                ; rdx = {+0x00 data, +0x10 len}
    41DBA0  mov eax,[rsi]                     ; LENGTH, not a msgid
    41DBAD  je 41DF4A                         ; zero length -> reject
    41DBB3  cmp [rdi+0x10],1 / jne 41DBCD
    41DBB9  test r12,r12 / je 41DF4A
    41DBC2  cmp eax,0x4F0 / ja 41DF4A         ; MAX WRITE SIZE = 1264 BYTES
    41DBCD  cmp [rdi+0x20],6 / jne 41DF4A     ; connection state gate
    41DBDC  lea r13,[rdi+0x250]
    41DBE3  cmp [r13],r13 / jne 41DE27        ; circular-list empty test
  accept path at 41DE27:
    41DE35  lea edx,[rbp+0x18] / call 41B7C0  ; alloc(len + 0x18) from pool
    41DE5E  call 24E8B70                      ; memcpy(node+0x18, src, len)
    41DE63..41DE7A                            ; tail insert onto conn+0x250
    41DE82  lock xadd [rdi+0x380], 1          ; queued node count
    41DE8D  lock xadd [rdi+0x390], len        ; queued bytes
    41DE9B/41DEA9                             ; lifetime totals +0x430/+0x448
    41DED7/41DEEC                             ; backpressure vs +0x384/+0x388
  No computed call, no vtable dispatch, no handler invocation anywhere.

0x41CF41 - THE DRAINER
    41CF46  lea r15,[rdi+0x250] / cmp [r15],r15
    41CF90  mov rsi,[r15]                     ; first node
    41CFA1  eax = [rsi-4] - [rsi-8]           ; len - cursor = remaining
    41CFA7  cmp eax,0x400 / cmovb ebp,eax     ; chunk = min(remaining, 1024)
    41CFBF  mov rcx,[rdi+0x18]                ; socket object
    41CFD9  call 2378CF0                      ; <-- send()
    41CFEA  lock xadd [rdi+0x428], sent       ; lifetime bytes sent
    41D057..41D07B                            ; unlink node
    41D07E/41D08C                             ; decrement count and bytes

  NODE LAYOUT: +0x00 cursor, +0x04 length, +0x08/+0x10 links, +0x18 data.
  CONNECTION LAYOUT: +0x10 type, +0x18 socket, +0x20 state, +0x250 TX queue
  head, +0x260 node pool, +0x380 count, +0x384 count cap, +0x388 byte cap,
  +0x390 queued bytes, +0x428 lifetime sent, +0x430/+0x448 lifetime totals.

  conn+0x20 == 6 is a TRANSPORT connection state, a different field from the
  session state at session+0x70.


################################################################################
##  SECTION 4: THE LOBBY WALL - WHAT THE CLIENT ACCEPTS
################################################################################

LAUNCH
    cd /d "<GAME>\Overwatch\_retail_"
    Overwatch.exe --tank_TournamentMode --lobbyServer=127.0.0.1:3724 --console --tank_logpath "<LOGS>"
  Tournament mode locks alt-tab; use the Windows key or Ctrl+Alt+Del.
  NOTE: production OWL never used --lobbyServer. Its launch line was
    --tank_tournamentmode --tank_locale <L> --tank_audiolocale <A>
    --tank_disablestreaming 1 --tank_disabledevdata 1
  and pod addresses came from showmodeLobbyLocations.json next to the exe.
  There is also --tank_tournamentmodeadmin, which runs OverwatchGM.exe (a
  separate binary, part of the _esports2_ flavor; check whether your install
  has it - UNTESTED).

THE LOBBY PROTOCOL - family 0x11D82194, EXACTLY FOUR MESSAGES
    msg 1  repeated u32 @0x78 (element size 4)
    msg 2  STRUCT(0x10) @0x78 + STRUCT(0x40) @0x88
           the second field's sub-table IS the ProtocolDescriptor
    msg 3  NO FIELDS (empty body)
    msg 4  NO FIELDS (empty body)
  m_messageMax = 5 is exclusive; the registration table at RVA 0x2CF0060 has
  exactly four entries keyed (message << 32) | 0x11D82194 for messages 1-4.
  Registration entry layout (0x20 bytes): +0x00 name ptr, +0x08 protocol ptr,
  +0x10 items ptr, +0x18 the packed key.

★ THE COMPLETE SERVER->CLIENT SURFACE AT STATE 6
  Tested across four independent harnesses (as the ack, as an immediate
  follow-up, as a poll response, and injected mid-stream after keepalives):
      TOLERATED (client survives to its 5s timeout):
          0x0003   keepalive; holds the socket indefinitely (44+ polls)
          0x0102   accepted as the ack. BODY IS NEVER PARSED - empty, 0x40
                   of garbage, wrong length, and a correct 0x50-byte
                   descriptor all behave identically
          0x0103   accepted; also holds the socket indefinitely
      LETHAL (instant teardown, 0 polls):
          EVERYTHING ELSE, in every position, with every body.
          Specifically ruled out: 0x0000, 0x0001, 0x0002, 0x0101, 0x0104,
          0x0105, 0x0106, 0x0107, 0x0108, 0x010A, 0x010C, 0x0200, 0x0201,
          0x0300-0x1000 (channel sweep at index 0), and a second 0x0102.
  ★ A DUPLICATE 0x0102 KILLS THE CONNECTION. Many earlier "body rejected"
    results were actually this: the harness sent a bare ack AND a second
    0x0102, and the duplicate was the cause, not the payload.

  Messages 1 and 4 were tested AS THE ACK with correct-shaped bodies
  (u32 zero/one/0xFFFFFFFF/family, and bare/empty for msg 4). All zero
  polls. Conclusion: messages 1 and 4 are CLIENT->SERVER ONLY.

BASELINE (do not re-measure)
  Bare 0x0102 ack + total silence = exactly 5 polls before close. Reproduced
  14/14. Any tolerated frame injected afterwards gives 4 polls - that
  one-poll difference is jitter inside the fixed ~5000ms timeout, confirmed
  by the hold test (0x0103 answered on every poll holds 44+ polls).

★ POOL INSTANCES DO EXIST AT THE LOBBY WALL  ★ CORRECTED rev 19
  rev 18 claimed a sweep found ZERO pool instances and concluded pools are
  built only after connect. THAT WAS AN ARTIFACT: the sweep matched the +8
  vtable addresses, which no object ever stores, so it could not have found
  anything either way.
  Re-run with exact pointer equality against the corrected (-8) addresses,
  confirmed by reading each vtable's crc getter:
      112 pool instances, 98 distinct protocols, stable across 10 passes.
  BOTH the lobby pool (11D82194) and ClientInLobbyConnect (F5FF548A) are
  live at the wall, with allocate_pool available.
  The ClientInLobbyConnect wire experiment still died instantly, but the
  reason is NOT "the pool does not exist" - that explanation is withdrawn and
  the real reason is unknown.

SESSION OBJECT (vtable RVA 0x2616260)
    +0x18  connection object pointer
    +0x40  INLINE ASCII "127.0.0.1:3724" (the address string starts here;
           rev 17 said +0x170 - both may hold copies, but +0x40 is what a
           live read shows)
    +0x5C, +0x74, +0x78, +0x98  all read ZERO for the entire session
    +0x60  status
    +0x70  state
    +0x1A0 cn (32 bytes, live-readable once nonzero)
  OBSERVED VALUES (rev 18, many runs):
    state:  0, 1, 2, 6      -- 7 HAS NEVER BEEN OBSERVED
    status: 0, 1, 2, 4, 5, 6
  The cycle is state 0/1/2 -> 6, status 6, then exactly 5000ms later status
  goes to 2 (reject) or 0, and it repeats. The timeout fires on schedule even
  when the socket is healthy and being keepalived - the keepalives hold the
  SOCKET, not the verdict.
  ★ "state 6 -> 7" in rev 17 was an INFERENCE, never an observation. There is
    no site anywhere in .text that writes the immediate 7 to a dword at
    [reg+0x70]; the state field is written from a register. Value-scanning
    for the writer is exhausted (the two candidate sites found belong to the
    replay/sequence-capture subsystem and to a decompressor).

Success() @ RVA 0xE38600 sets session+0x60 = 7 and CAN be called from an
  injected DLL with the session as 'this'. It advances the screen to a
  continuous "ENTERING GAME" but does NOT move state (+0x70). The completion
  callback 0xE38590 consumes the verdict and resets status to 0.
  State and status are different axes; forcing the verdict cannot substitute
  for whatever advances state.


################################################################################
##  SECTION 5: REAL BLIZZARD TRAFFIC
################################################################################

research/cli_dec.bin - client->server, 211 frames, 61,712/61,712 bytes
research/srv_dec.bin - server->client, 94 frames, 383,890/383,890 bytes
  ★ srv_dec.bin parses only from offset 292 (the first 292 bytes are the
    decrypted state blob). cli_dec.bin parses from 0.

FRAME FORMAT
    [u24be frame_len][u16be msgid][ ... ]
  The third field is msgid-dependent - a u32le length for some, a counter for
  others. Bare two-byte msgid-only frames are normal and legitimate.

BODY FORMAT
    [u32le block_len][block] repeated, interleaved with fixed fields.
  356 such blocks found across the 94 server frames with a strict validator;
  one decodes to a real player BattleTag, confirming the reading.
  16-byte entity handles are [u32le id][8 zero bytes][u32le type]; the
  session's primary handle occurs 37 times across both directions.

THE OPENING EXCHANGE (retail)
    C->S  0x0000  334 B   client connect
    S->C  0x0002    2 B   BARE ACK - msgid only, no payload
    C->S  0x1200  730 B   client proceeds on its own
    S->C  0x3004  106 B
    S->C  0x3005  21162 B (map/asset manifest)
    S->C  0x4e0d  x4      identity + account data
  The retail server ALSO sends its ack bare, which independently confirms
  message 2 is meant to carry no body there.

CHANNEL INVENTORY (server->client, retail): 28 distinct channels -
  0x00 0x01 0x03 0x05 0x08 0x0e 0x0f 0x11 0x13 0x1c 0x20 0x24 0x25 0x27 0x28
  0x29 0x2a 0x2e 0x30 0x31 0x32 0x37 0x39 0x3d 0x40 0x47 0x4d 0x4e

WHY RETAIL DOES NOT TRANSPLANT
  - The 1.74 family 0x11D82194 appears ZERO times in the retail capture.
  - Retail's 0x01xx run is family 0x48CBF320, which does not exist in the
    1.74 binary at all.
  - A two-session diff proved essentially every byte inside a frame is
    session-specific.
  The capture is good for: the key schedule, the frame grammar, proof that
  bare frames are legitimate, the channel encoding, and a template of what a
  working session looks like.

AVAILABLE CAPTURES AND THEIR KEY STATUS
    packets.pcap          FULL SESSION, k0-k3 PUBLISHED. This is the source of
                          cli_dec/srv_dec. difficulty 9.
    login-enc.pcapng      FULL SESSION incl. handshake, 2.5 MB of lobby
                          traffic (6x the decrypted one), plus 400 KB on 443
                          and Battle.net RPC on 1119. NO KEYS - MAC1 does not
                          verify with zeros, so real session keys. difficulty 7.
    arcade_tdm_cancel / competitive / custom_game / queue_qp  (4 files)
                          MID-SESSION, no handshake, no keys. These capture a
                          client populating arcade panels, custom-game
                          browsers and lootboxes - i.e. exactly the
                          post-connect flow we want. arcadecontents and
                          customgamepopular are the decoded ground truth of
                          what is inside them.
  ★ HIGHEST-VALUE OUTSTANDING ASK: the k0-k3 slots for any of those five.
    The pipeline is proven end to end, so keys turn any of them into
    plaintext immediately. Whoever published packets.pcap with its keys
    (AyakaPS / Plasmawatch, 2024-05-19) may have the rest.
  ★ STILL MISSING: a TOURNAMENT-MODE capture. All five are retail sessions
    with real keys. A tournament capture would have zeroed keys and decrypt
    with the tooling already in this repo.


################################################################################
##  SECTION 6: CASC / TACT KEYRING  ★ NEW, rev 18
################################################################################

MOTIVATION
  Prometheus (BreakingBread0) reached the OW 0.8 lobby screen by (a) emplacing
  all the CASC decryption keys the client expects and (b) calling the JAM
  handler that loads the data packs. His talk: media.ccc.de,
  "Prometheus: Reverse-Engineering Overwatch", 39C3, 2025-12-28. The relevant
  passage: "if that JAM message emplaces CASC keys streamed by the server, why
  don't we just call it with the keys it expects? ... And we're in the lobby
  screen."
  ★ TESTED ON 1.74 AND IT IS NOT OUR BLOCKER - see the result at the end.
    The machinery below is documented because it works and because the CALL
    technique it proves is the foundation of any future local-injection work.

THE KEYRING (from 3rdparty\Battle.net.labs\TACT\tact_core\source\Keyring.cpp)
  Instance is at  storage_object + 0x340
      keyring +0x08   entry vector base pointer
      keyring +0x10   entry count
  ENTRY LAYOUT, 0x30 stride:
      +0x00  id length byte      (8 for a TACT key)
      +0x01  id bytes            (stored BYTE-REVERSED vs the hex string)
      +0x18  value length byte   (0x10 for a TACT key)
      +0x19  value bytes         (stored in string order)
  FUNCTIONS:
      0x391DD0  Keyring::LoadFromBuffer(this, buf, len) -> 0 on success,
                0x10 on parse failure
      0x39194A  AddKey with duplicate check (compares length then memcmp)
      0x3918D0  linear find-by-id over the 0x30-stride array
      0x391C88  GetKey / lookup (NO direct callers - virtual)
      0x392080  per-line parser

  ★ TEXT FORMAT the parser accepts (this is NOT the overtools keyring format):
        key-<KEYNAME> = <KEYVALUE>
    - lines beginning '#' are comments and are skipped
    - '##' at the very start of a line skips two characters
    - leading and trailing whitespace is trimmed
    - the identifier MUST begin with the literal 4-byte prefix "key-"
      (constant at RVA 0x25EBFA0); the remainder is hex-decoded as the id
    - separator may be whitespace or '='
    - empty lines are skipped, so a trailing newline is fine
    - KEYNAME must be BYTE-REVERSED relative to the overtools keyring file
      (overtools 57A5A33B226B8E0A -> client key-0A8E6B223BA3A557)

INJECTION - WORKS, VERIFIED
  owobs v127/v128: locate the keyring by entry signature (id length 8, value
  length 0x10, 0x30 stride, id matching a known key name), then call
  LoadFromBuffer with a blob of 170 keys. Result:
      returned 0 (SUCCESS), count 63 -> 185 (+122, 48 deduplicated)
      verify walk: recognised LE=170, BE=0, unrecognised=15
  The client's own duplicate check fired correctly once the byte order was
  right, which is the proof the representation matches what its lookups use.
  Keys do NOT persist - they live in the process heap and must be re-injected
  every launch.

WHERE THE KEYS COME FROM
  github.com/overtools/OWLib -> TankLib/Overwatch.keyring
  305 keys, all enabled, 2016 through OW2 2026. The first 170 are OW1-era and
  the last of those is literally "[1.74] Genji OWL Skin". Every content key a
  1.74 client could need is public.

★ 15 KEYS YOUR CLIENT HAS THAT THE PUBLIC LIST DOES NOT
  The 1.74 build config carries 63 keys, of which 15 are absent from
  overtools' list. Six of them are byte-for-byte the keys Prometheus hardcodes
  for 0.8 (including ones his comments mark "streamed from server"):
      FB680CB6A8BF81F3  62D90EFA7F36D71C398AE2F1FE37BDB9
      11A9203C9881710A  2E2CB8C397C2F24ED0B5E452F18DC267
      A19C4F859F6EFA54  0196CB6F5ECBAD7CB5283891B9712B4B
      87AEBBC9C4E6B601  685E86C6063DFDA6C9E85298076B3D42
      DEE3A0521EFF6F03  AD740CE3FFFF9231468126985708E1B9
      49166D358A34D815  667868CD94EA0135B9B16C93B1124ABA
      5E152DE44DFBEE01  E45A1793B37EE31A8EB85CEE0EEE1B68
      CAE3FAC925F20402  98B78E8774BF275093CB1B5FC714511B
      (plus 7 more; dump the keyring with owobs v123 to list all 15)
  These are worth contributing back to overtools/wowdev regardless of how
  this project ends.

★ RESULT: KEYS ARE NOT THE 1.74 BLOCKER
  With 185 correctly-formed keys installed and verified, the session behaves
  IDENTICALLY: state 6, status 6, 5000ms, timeout, repeat. No change.
  Why the 0.8 analogy does not hold:
    - In 1.74 nothing streams keys into the keyring. AddKey has exactly one
      caller (LoadFromBuffer) and LoadFromBuffer has exactly one caller (the
      TACT config init at 0x33F50A). There is no JAM message that feeds it.
    - Your install is a complete retail build that shipped with its data packs
      and the 63 build-config keys those packs need. CASC already works.
  0.8 was a beta genuinely missing keys. 1.74 is not.


################################################################################
##  SECTION 7: WHAT PROMETHEUS ACTUALLY DOES (corrected)
################################################################################

Rev 17 Section 5 said Prometheus is "client-side state forcing ... nothing to
port". That undersells it and gets the mechanism wrong.

Prometheus does NOT implement a server. It writes directly into the client's
own data structures and calls the client's own functions:
    - TACT keys: walks storageHandler -> +0x50/+0x58, calls the game's
      emplace function at gameBase+0x9d0470 per key, memcpy's the key in,
      sets a validity byte. (0.8 addresses.)
    - JAM: hooks addJamProtocolPool and associatePool to enumerate pools and
      their receiver functions, then calls the JAM handler directly.
    - The gameplay shown in the talk comes from state_replicator, peer-to-peer
      entity replication between two patched clients. No lobby involved.

WHAT PORTS TO 1.74
    - The STRUCTURES: JAM.h's layouts match ours field-for-field.
    - The TYPE ENUM (Section 2).
    - The METHOD: heap writes + calling game functions. Both work under
      Arxan; we have now proven the call path twice.
WHAT DOES NOT PORT
    - Every address (0.8 build).
    - MinHook inline hooking of internal functions.
    - The specific fix (missing CASC keys), per Section 6.

★ THE OPEN PROBLEM WITH THE LOCAL-INJECTION ROUTE
  Prometheus finds handlers by hooking associatePool at runtime and reading
  receiverInstance+0x20 per pool CRC. That works on 0.8 because 0.8 boots to a
  menu with pools alive and handlers registered. On 1.74 NO POOL INSTANCES
  EXIST at the wall (Section 4), so there are no handlers to enumerate, and
  the dispatch function's strings ("Unhandled JamMessage, unable to dispatch"
  @ RVA 0x2609A10, "Invalid Protocol Type" @ 0x2609B78, "_msgID" @ 0x2609B0C)
  have ZERO resolvable xrefs - no rip-relative lea, no stored qword pointer
  anywhere in the image. They are reached through the obfuscated path.
  A possible way through, untried: we have all 105 pool vtables, so we could
  construct a pool instance ourselves (allocate, plant the vtable pointer) and
  call allocate_pool at slot 6 to have the CLIENT build a properly-formed
  JamMessage, then populate fields at the offsets we know. That still leaves
  the handler to call.


################################################################################
##  SECTION 7B: THE JAM ROUTER AND ASSOCIATION MAP  ★ NEW, rev 19
################################################################################

All of this is disassembly-derived and does not depend on any memory scan.

THE ROUTER
    0x560D90   route(rcx = subsystem, rdx = JamMessage*) -> bool in al
  Its second .pdata chunk 0x560DDF does:
      call 0x565240              ; crc = msg->get_message_info()->crc
      cmp  eax, 0x11D82194       ; lobby special case
      jne  0x560E4D              ; -> error path (builds 0x800000000000000F)
      lea  r8, [r14+0xC0]        ; the association map
      ... BST lookup, see below ...
      jne  0x560EDC              ; found -> dispatch
  r14/r15 are set at 0x560DB2: `mov r14, rcx` / `mov r15, rdx`.

THE ASSOCIATION MAP at subsystem+0xC0 - a plain BST, NOT std::map
      root      = *(map + 0x00)
      left      = node + 0x08
      right     = node + 0x10
      key       = node + 0x20   (u32 protocol CRC)
      NULL-terminated; the map address itself is the end() sentinel
  The value offset is NOT established - see the warning in Section 8.

JamMessage ACCESSORS (all go through vtable slot 2 = get_message_info)
    0x565240   -> info->[0x18]  crc
    0x565260   -> info->[0x1C]  message_id (u16)
    0x565280   -> info->[0x00]  message_name
  This independently confirms the JamMessageInfo layout in Section 2.

LOCAL DISPATCH TEMPLATE (observed at 0xE3C720, a real caller)
      lea  rcx, [rsp+0x30]
      call <ctor>              ; e.g. 0x4F3210: base ctor 0x565080,
                               ;   plant vtable, zero +0x58; returns the obj
      lea  rdx, [rsp+0x30]
      mov  rcx, [*(this+0x40)] ; the subsystem
      call 0x560D90            ; route
  Eight direct callers of 0x560D90 exist; in most of them rcx is the
  caller's own `this`, i.e. those functions are methods of the subsystem.
  0x18F5D70 obtains the subsystem by NAME:
      0x56B470(manager, &out, "<name>", 3)   ; r8 is a C string, strlen'd
  "LobbySystem" is at RVA 0x2615330.

ALL 922 MESSAGE CLASS VTABLES ARE RESOLVED
  Every message class exposes get_message_info() at vtable slot 2 as a
  two-instruction `lea rax,[rip+X]; ret`, so all 922 were mapped statically
  to their (crc, message_id):
      lobby msg 1  vtable 0x2609CD8      msg 2  0x2609CF8
            msg 3  0x2609D18             msg 4  0x2609D38
      ClientInLobbyConnect 20500 0x25FEB00 .. 20509 0x25FEC20
  (Full table: /home/claude/msgvtables.pkl regenerable from the dump.)

WHAT IS STILL MISSING FOR LOCAL DISPATCH
  Only the runtime subsystem pointer. Everything else - pool instances,
  allocate_pool, message vtables, schemas, the router - is in hand.
  NEXT IDEA (untried): pool instances are reachable FROM the association
  map, so scan for a qword equal to a KNOWN pool instance address and walk
  back to the containing structure. That is pointer equality, the only
  technique that has not produced false positives on this binary.

################################################################################
##  SECTION 7C: MEMORY-SCAN FALSE POSITIVES - READ BEFORE SCANNING  ★ rev 19
################################################################################

Four consecutive scanning approaches produced confident-looking false
positives in one session. This binary is unusually hostile to value-based
heuristics because several large regions are DENSE in exactly the constants
we search for.

  v129  "3 heap pointers + a known CRC at +0x20"      -> telemetry text
  v130  "{ptr,count} + BFS + >=3 known CRCs"          -> same region
  v131  same, aligned dump                            -> per-protocol latency
                                                         log ("+ 647 ms",
                                                         "127.0.0.1"), 0x30
                                                         stride
  v133-135 "BST walk + strictly increasing known CRCs" -> a SORTED ARRAY of
                                                         protocol CRCs next to
                                                         audio device strings
  The sortedness check does not discriminate: a sorted CRC array satisfies it
  trivially. 86 of 87 "map entries" failed self-validation once we required
  the pointed-to object to REPORT ITS OWN KEY.

RULE: a signature keyed on VALUES finds whatever region is dense in those
values. Only two things have worked reliably on this target:
  1. exact pointer equality against a known address (v136, pool instances)
  2. self-validation - make the data confirm itself (follow the pointer, read
     the object's own crc getter, require it to equal the key you matched on)
Always include (2) even when (1) looks conclusive.

################################################################################
##  SECTION 8: VOID - DO NOT USE
################################################################################

VOICECHAT, NOT LOBBY (accurately decoded, wrong subsystem):
    0x86BEE0   VC ResponseConnect handler
    0x86F432   VC ResponseLogin handler
    0x85FFE0   VC pending-connect map lookup
    0x261AB68  VC message names table
    0x2F53030 / 0x2F63214 / 0x2D0C818 / 0x2D0C848
  VOID THEORY: "the server must echo an 8-byte pending-connect token". Real -
  in the voice chat client. No evidence the lobby works this way.
  VOID TOOLING: owobs v41 through v47 all hunted the VC pending map.

MISLABELLED FUNCTIONS:
    0x4A0450   NOT a parse function. A vector<u32>::resize thunk.
    0x56CA50   NOT a parse function. A vector<u64>::resize thunk.
    0x41B7C0   NOT a handler lookup. A size-class ALLOCATOR.
    0x41DB60   NOT a dispatcher. TX queue enqueue (Section 3).
    0x439904   NOT a drain+dispatch core. A small bool predicate.

SUBSYSTEMS THAT LOOK RIGHT AND ARE NOT:
    +0x2C9Fxxx 6-message table - a different subsystem, verified against the
               family table. Near-repeat of the VoiceChat trap.
    0x84xxxx state machine (writes 6, 7, 8 to [reg+0x70]) - this is the
               REPLAY / SEQUENCE CAPTURE subsystem. Its strings are
               "Begin Sequence capture", "Couldn't create sequence entity",
               "Playback complete", "End Sequence capture". The session's
               state never passes through 3, which that machine writes
               immediately after storing its entity id. NOT the lobby.

METHOD NOTE: before building anything on a table or handler, VERIFY which
family/subsystem owns it. Three separate multi-day detours in this project
came from skipping that check.


################################################################################
##  SECTION 9: RULED OUT - DO NOT RETRY
################################################################################

- PROACTIVE PUSH BEFORE CONNECT. Races the client's own connect; teardown.
- FORCING THE VERDICT (session+0x60 = 7). Advances the screen, never state.
- DERIVING A TOKEN FROM THE HANDSHAKE NONCES. (Also chasing the VC token.)
- INLINE CODE HOOKING INSIDE Overwatch.exe. Blanket VirtualProtect denial.
- HARDWARE BREAKPOINTS. Even a one-shot arm kills the process.
- x64dbg + ScyllaHide.
- JSON DECODE PATH. 15 variants, all flat 5s timeouts. Protocol type is fixed
  to binary from the server side.
- CONNECT-MIRROR SHAPES. 6 variants, all flat timeouts.
- RETAIL BODIES TRANSPLANTED (0x3004, the 898-byte 0x0102, etc). Wrong family;
  bodies are session-specific.
- MSGID SWEEPS. The tolerated set in Section 4 is now established four ways.
  DO NOT BUILD ANOTHER SWEEP.
- CHANNEL SWEEP at message index 0 (0x0000, 0x0200..0x1000). All lethal.
  Explained by "no pools exist at the wall".
- CASC KEY INJECTION as a route to the menu (Section 6). Works mechanically,
  changes nothing.
- VALUE-SCANNING FOR THE state/status WRITER. Both fields are written from
  registers; no immediate-write site exists for the session object.
- STATIC XREFS TO POOL CONSTRUCTION. Pool vtable addresses are never
  referenced by lea and never stored as a qword; they are computed under
  obfuscation (the same movabs+xor pattern seen at 0x842DBF).

- CLIENT-SIDE STATE FORCING A LA PROMETHEUS: superseded, see Section 7. The
  earlier claim that "their code contains no login/connect/server logic so
  there is nothing to port" was wrong in spirit - the structures and the
  method port; the specific fix does not.


################################################################################
##  SECTION 10: OPEN QUESTIONS / WHERE TO GO NEXT
################################################################################

Ordered by estimated value.

1. GET KEYS FOR ONE MORE CAPTURE.
   The decryption pipeline is proven end to end. k0-k3 for login-enc.pcapng
   or any of the four gameplay captures converts megabytes of real
   post-connect traffic into plaintext, including a client populating arcade
   and custom-game menus. This is the single highest-value input available and
   it costs one question to the right person.

2. A TOURNAMENT-MODE CAPTURE.
   Still the artifact that would end the project. Zeroed keys, decryptable
   with what is already in this repo. Anyone who ran playertools at an OWL or
   Showmatch event in 2022 may have one.

3. FIND THE SUBSYSTEM POINTER (supersedes rev 18's "find the dispatch").
   The dispatch IS found (Section 7B). Only route()'s first argument is
   missing. Scan for a known pool instance address as a VALUE and walk back
   to its container.

3b. (rev 18 framing) FIND THE JAM DISPATCH FUNCTION.
   "One big switch statement, second argument the already-deserialized
   JamMessage" (per the 39C3 talk). Locating it would allow calling handlers
   directly and bypass the wire entirely - which is the only approach that has
   been shown to work on any build.
   Blocked on: no xrefs to its strings, no pool instances at the wall.
   Untried angles: reconstructing a pool instance from a known vtable and
   calling allocate_pool (slot 6); locating the switch by its jump table
   (needs tighter constraints than "dwords in .text range" - try requiring the
   targets to lie within one .pdata function).

4. RECOVER THE WIRE ENCODING.
   The schemas give field types and order; the serialisation is unknown and
   is NOT a flat struct dump. Without it no constructed body can be correct.
   The per-field +0x20 pointers are vector-resize thunks, not codecs, so the
   encoder is generic and lives in the decode/dispatch path.

5. OverwatchGM.exe / --tank_tournamentmodeadmin.
   A separate binary implementing the same protocol, possibly with different
   Arxan coverage. Check whether your install has it. Completely untested.

6. THE 292-BYTE BLOB TRAILER.
   We send 36 zeros; the real one carries structure. Sidiusz confirms zeros
   reach state 6, so this is not the wall, but it is the one place we
   knowingly send something a real server never would.

7. LOOTBOXES. Not started. Downstream of everything above.


################################################################################
##  SECTION 11: TOOLING REFERENCE
################################################################################

FILES (place next to Overwatch.exe, i.e. <GAME>\Overwatch\_retail_\)
    bink2w64.dll            proxy loader (replaces the original)
    bink2w64_original.dll   the real Bink DLL, renamed
    cainj.dll               cert/CA injection (only for the bnet route)
    owobs.dll               observer / scanner / injector

BUILD (MSVC x64 Developer Command Prompt, from <TOOLS>)
    cl /nologo /LD /O2 /D_CRT_SECURE_NO_WARNINGS /Fe:owobs.dll owobs_vNNN.cpp
  Copy the DLL to <GAME>\Overwatch\_retail_\ after EVERY build. Check the
  version banner in the log before trusting a run.

KEY OWOBS VERSIONS (rev 18 generation)
    v112  socket watcher (TCP table + session state)
    v116  session state watcher - logs state/status on change. The single
          most useful instrument for any wire experiment.
    v117  JAM pool enumerator by vtable getters
    v118  raw dump of .rdata pointer windows + getter bytes, for offline
          disassembly. 37 MB output; this is how the 105 pools were recovered.
    v119  JAM schema dump - 922 message registrations with field layouts
    v120  pool instance finder - VOID, used the +8 vtable addresses
    v123  keyring finder (read-only)
    v127  keyring injector, correct format and byte order
    v128  injector + session watch
    v129-v135  association-map hunts - ALL FALSE POSITIVES, see Section 7C
    v136  pool instance finder by exact vtable match (-8). 112 instances,
          98 protocols, incl. lobby and ClientInLobbyConnect

LOBBYSERV MODES (rev 18 generation, v146-v158)
    baseline     ack then silence; measures the 5-poll budget
    holdtest     answer every poll with one msgid; measures socket hold
    chain        keepalives with one candidate injected per poll; the
                 teardown identifies the killer. The efficient sweep harness.
    descriptor / ackmsg  one candidate per connection as the ack itself
    lobbyconnect ClientInLobbyConnect sequence on a configurable channel
  In-memory candidate counters (no state file) - restart lobbyserv to reset.

OFFLINE ANALYSIS
    Doc addresses == RVAs == file offsets in the pe-sieve dump.
    .pdata at 0x34D8000 gives 165,412 function boundaries.
    capstone for disassembly; the dump's .text and .rdata are complete.

SERVER
    lobbyserv.py   raw TCP + JAM on 127.0.0.1:3724
    bnetserv.py    HTTPS stub on 127.0.0.1:443 for the non-tournament route
    bnetprobe.py   passive listener on 443/1119/80 that logs connections and
                   TLS SNI without terminating TLS - use it to verify the
                   client actually dials Battle.net before building a stub

BNET / CERT ROUTE (alternative, converges on the same wall)
  Skip --tank_TournamentMode. hosts entries -> 127.0.0.1 for
  us.actual.battle.net, eu.actual.battle.net, account.blizzard.com,
  oauth.battle.net, us.battle.net. Leave telemetry hosts alone.
  cainj.cpp injects a CA into the live cert store and zeroes a verify_mode
  field - both DATA writes, which is why it survives Arxan.
  Load ONLY cainj.dll on this path; hook_probe.dll and protect_sweep.dll
  crash the client here.
  STATUS: Sidiusz solved the cert wall by patching the heap-parsed pin/verify
  result rather than adding a CA, reached the actual web-auth login form, and
  then hit the identical lobby wall. This route does not avoid the problem.


################################################################################
##  SECTION 12: COMMUNITY / RELATED WORK
################################################################################

Independent implementers who reached this exact wall:
  - Sidiusz - reconstructed the same handshake field-for-field, solved the
    cert pinning, reached the web-auth login form, stuck at the same point.
    Confirmed 2026-08-09: "I don't have it either" re: the reply to 0x0103.
    Stopped work on OW1 in August 2026.
  - Zagrion (AyakaPS) - contributed the s2c key derivation and the tshark
    reassembly method that made srv_dec.bin possible.
  - iEvE - has PRIVATELY reached the main menu on 1.74 in TOURNAMENT MODE
    (screenshot dated 2024-11-25, tournament mode confirmed in the same
    thread). Will not share until his server stops being supported. His
    existence is proof the second message is constructible from this client.

Projects worth reading:
  - Prometheus, github.com/BreakingBread0/prometheus (AGPL; the older
    saturn-xvi/prometheus artpop2 branch is the same project). See Section 7.
    Talk: media.ccc.de "Prometheus: Reverse-Engineering Overwatch", 39C3.
    Contact: contact@breakingbread.at - publicly invites contact, and is the
    one person with deep JAM knowledge who has no server to protect.
  - overtools/OWLib + overtools/TACTLib - the Overwatch keyring and CASC
    reader. TankLib/Overwatch.keyring is where the TACT keys come from.
  - wowdev.wiki/TACT and wowdev/TACTKeys - TACT format and key list.
  - zkxjzmswkwl/hello-kitty-island-adventure-private-server - leaked OWL
    playertools.ps1. Source of showmodeLobbyLocations.json, the
    prometheus_tournament5 product code and the _esports2_ flavor. No
    protocol content, but it has the production launch arguments.
  - Gophercraft - bnet RPC envelope format.

================================================================================
END OF DOCUMENT
================================================================================
